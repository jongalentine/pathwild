# ============================================
# PathWild Environment Variables Template
# ============================================
# Copy this to .env and fill in your actual values
# .env is gitignored (never commit secrets!)

# AWS Credentials
# IMPORTANT: Use AWS CLI (aws configure) instead of storing here
# Only set these if AWS CLI doesn't work
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_DEFAULT_REGION=us-west-2

# API Keys
NOAA_API_KEY=your_noaa_key_here
# GOOGLE_MAPS_API_KEY=your_google_key  # For future terrain features

# Database (for future use)
# DATABASE_URL=postgresql://user:pass@localhost/pathwild

# Flask Configuration
FLASK_APP=app.py
FLASK_ENV=development
SECRET_KEY=change_me_in_production_use_random_64_char_string

# MLflow
MLFLOW_TRACKING_URI=file:./mlruns

# Feature Flags
ENABLE_MPS_ACCELERATION=true
ENABLE_EXPERIMENTAL_FEATURES=false

# PathWild Metadata
PATHWILD_VERSION=0.1.0
